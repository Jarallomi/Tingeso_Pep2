spring:
  devtools:
    restart:
      aditional-paths: src/main/java
  application:
    name: subir-archivo
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    plataform: postgres
    driver-class-name: org.postgresql.Driver
    dbname: archivos
    url: jdbc:postgresql://localhost:5432/archivos
    username: postgres
    password: 2905
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081